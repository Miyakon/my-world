pipeline {

    agent any

    environment {
        fileName = "output.txt"
    }

    stages {

        stage('write file') {
            steps {
                writeFile(file: fileName, text: "output text")
            }
        }

        stage('archive artifacts') {
            steps {
                archiveArtifacts fileName
            }
        }
    }
}